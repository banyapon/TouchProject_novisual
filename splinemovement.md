# splinemovement.cs ทำอะไร

`splinemovement` เป็น `MonoBehaviour` สำหรับพาวัตถุเคลื่อนที่ไปตามถนนที่สร้างด้วย `Spline` โดยใช้ท่าทางสัมผัส 3 แบบหลัก:

- `Drag` แนวตั้งเพื่อเดินหน้า/ถอยหลัง
- `Swipe` แนวนอนเพื่อเลือกทางเลี้ยวล่วงหน้าเมื่ออยู่ใกล้ทางแยก
- `Two-finger drag` เพื่อหมุนมุมมอง/ทิศหมุนเพิ่มจากภายนอกเส้นทาง

สคริปต์นี้ต่างจาก `dogpaddle` ตรงที่ไม่ได้เดินในโลกแบบอิสระ แต่จะถูกบังคับให้อยู่บนเส้นถนน (`Spline`) ตลอดเวลา

## องค์ประกอบหลัก

- `roadNetwork` ใช้อ้างอิง `RoadNetworkSplineCreator` สำหรับถามตำแหน่ง ทิศทาง และการเปลี่ยนถนน
- `Rigidbody` ใช้ขยับวัตถุแบบ kinematic
- `carState` เก็บสถานะปัจจุบันของรถ/ตัวละครบนเครือข่ายถนน
  - `roadNo` ถนนปัจจุบัน
  - `currentPos` ตำแหน่งบนถนน
  - `dir` ทิศการวิ่ง
  - `currentLane` เลนหรือทางเลือกถัดไปที่จองไว้
- `LineRenderer` ใช้วาดเส้นไฮไลต์ถนนแต่ละเส้น

## ลำดับการเริ่มต้น

ใน `Start()`

1. ดึง `Rigidbody` และตั้งให้
   - ไม่ใช้แรงโน้มถ่วง
   - ไม่หมุนด้วยฟิสิกส์
   - เป็น `isKinematic = true`
2. หา `RoadNetworkSplineCreator` ถ้ายังไม่ได้กำหนด
3. ถ้าไม่พบ `roadNetwork` จะปิดสคริปต์
4. ดึง `SplineContainer`
5. สร้างค่าเริ่มต้นของ `carState`
   - เริ่มที่ `roadNo = 1`
   - เริ่มตำแหน่งต้นถนน
   - `dir = 0`
   - `currentLane = 0`
6. เรียก `ApplyDefaultLane()` เพื่อเลือกเลนเริ่มต้น
7. สร้าง `LineRenderer` สำหรับทุก spline
8. วางวัตถุบนถนนทันทีด้วย `PlaceOnRoad(true)`
9. อัปเดตสีไฮไลต์ด้วย `UpdateHighlight()`

## การเคลื่อนที่หลัก

การทำงานหลักเกิดใน `Update()`

1. อ่าน input ด้วย `HandleTouchInput()`
2. ถ้ามีการหมุนแบบ animation อยู่ จะค่อย ๆ หมุนไปยัง `targetRotation`
3. เรียก `PlaceOnRoad(false)` ทุกเฟรมเพื่อบังคับให้วัตถุอยู่บน spline
4. ถ้าถนนปัจจุบันหรือสถานะทางแยกเปลี่ยน จะอัปเดตการไฮไลต์เส้น

## เดินเหมือน dogpaddle ตรงไหน

การเดินใช้ `Drag` แนวตั้งเหมือนแนวคิดใน `dogpaddle`

- ใน `OnTouchMoved()` ถ้า drag มากกว่า dead zone จะถูกมองเป็น `Drag`
- สคริปต์แปลง `touch.deltaPosition` จาก pixel เป็นเซนติเมตรด้วย `PixelsToCm()`
- ถ้ามีการขยับในแกน Y จะเรียก `MoveOnRoad(frameCm.y)`

ใน `MoveOnRoad()`

1. คำนวณระยะเดินจากความยาว drag, `cmToSpeedScale`, `baseSpeed`, และ `Time.deltaTime`
2. ถ้า `frameDeltaCm_y < 0`
   - มองเป็นการปาดลง
   - จะเดิน `Forward`
   - คอมเมนต์ในโค้ดระบุชัดว่าให้เหมือน dog-paddle stroke
3. ถ้า `frameDeltaCm_y >= 0`
   - จะเดิน `Backward`
4. การขยับจริงทำผ่าน `roadNetwork.MoveCarLoop(carState, dist, mode)`

สรุปคือ:

- รูปแบบ input สำหรับการเดินคล้าย `dogpaddle`
- แต่ปลายทางของการเคลื่อนที่จะถูกล็อกบนเส้น spline เสมอ

## การ Detect Swipe เพื่อเลือกทาง

มีการ detect `Swipe` จริงใน `OnTouchEnded()`

เงื่อนไขที่จะถูกนับเป็น swipe:

- ความเร็วมากพอ (`velocity >= swipeVelocityThresholdCm`)
- เวลาสั้นพอ (`elapsed <= swipeMaxDuration`)
- ระยะมากพอ (`finalDist >= swipeMinDistanceCm`)
- ระยะแกน X มากพอ (`abs(finalPx.x) >= swipeMinPixels`)
- เป็นแนวนอนมากกว่าแนวตั้ง (`abs(finalPx.x) > abs(finalPx.y)`)

ถ้าเป็น swipe และกำลังอยู่ในเขตทางแยก (`IsAtJunction()`)

1. ตั้ง gesture เป็น `Swipe`
2. ตรวจว่าปัดซ้ายหรือขวา
3. เรียก `roadNetwork.ChangeLane(carState, swipeDir)`
4. ถ้า lane เปลี่ยนจริง จะเรียก `UpdateHighlight()`

ความหมายของพฤติกรรมนี้คือ:

- ผู้เล่นสามารถ "จองเส้นทางล่วงหน้า" ได้ตอนใกล้ทางแยก
- ยังไม่เลี้ยวทันทีในจังหวะ swipe
- การเลี้ยวจริงจะเกิดเมื่อวิ่งต่อไปจนข้ามไปยังถนนถัดไปตาม lane ที่เลือกไว้

## การรู้ว่าอยู่ใกล้ทางแยกหรือไม่

ฟังก์ชัน `IsAtJunction()` ใช้ตรวจว่าขณะนี้อยู่ในช่วงปลายถนนที่สามารถเลือกทางได้หรือยัง

- ถ้าวิ่งทิศ `dir == 0`
  - จะดูว่า `t` ใกล้ปลายถนนหรือยัง
  - และต้องมีทางออกใน `laneE`
- ถ้าวิ่งอีกทิศ
  - จะดูว่า `t` ใกล้ต้นถนนหรือยัง
  - และต้องมีทางออกใน `laneS`

`junctionNormalizedRadius` เป็นตัวกำหนดความกว้างของช่วงที่ถือว่า "อยู่ในทางแยก"

## การเลือก lane เริ่มต้น

`ApplyDefaultLane()` จะเลือกว่า lane เริ่มต้นควรเป็นช่องไหน

- ถ้ามีทางออก 2 ทางหรือน้อยกว่า ใช้ lane `0`
- ถ้ามี 3 ทาง ใช้ lane กลาง คือ `1`
- ถ้ามากกว่า 3 ทาง ใช้ค่ากลางของจำนวน lane

จุดนี้ทำให้แม้ผู้เล่นยังไม่ swipe ระบบก็ยังมีทางที่เลือกไว้โดยค่าเริ่มต้น

## การเลี้ยวจริงเกิดเมื่อไร

เมื่อ `MoveOnRoad()` พาวัตถุเคลื่อนที่จนเปลี่ยน `roadNo`

1. เรียก `ApplyDefaultLane()` สำหรับถนนใหม่
2. เรียก `StartRotateToCurrentRoad()`
3. ให้ตัวละครค่อย ๆ หมุนตาม tangent ของถนนใหม่

ดังนั้น flow คือ:

- ปัดซ้าย/ขวาเพื่อเลือก lane
- เดินต่อเหมือน dogpaddle
- เมื่อถึงจุดเปลี่ยนถนนจริง จึงค่อยหมุนตามเส้นทางใหม่

## การวาดเส้น spline

`BuildLineRenderers()` จะสร้าง `LineRenderer` หนึ่งตัวต่อหนึ่ง spline

- ใช้ `lineResolution` จุดในการวาดแต่ละเส้น
- ใช้ `lineWidth` เป็นความหนา
- วาดเป็น world space
- พยายามใช้ shader แบบ unlit ก่อน

## การไฮไลต์สีของเส้นทาง

`UpdateHighlight()` จะระบายสีให้แต่ละถนนตามสถานะ

- ถนนปัจจุบัน ใช้ `currentRoadColor`
- ถนนถัดไปที่เลือกไว้ ใช้ `nextRoadColor`
- ถนนอื่น ใช้ `idleRoadColor`

จากค่าที่ตั้งไว้ในโค้ดตอนนี้:

- `currentRoadColor = เหลือง`
- `nextRoadColor = เขียว`
- `idleRoadColor = เทาโปร่ง`

ดังนั้นตามโค้ดปัจจุบัน:

- เส้นที่กำลังวิ่งอยู่จะเป็นสีเหลือง
- เส้นทางถัดไปที่เลือกไว้ล่วงหน้าจะเป็นสีเขียว

ถ้าต้องการให้ "เวลาจะเลี้ยว เส้นทางที่จะไปเป็นสีเหลือง" ต้องแก้ค่าของ `nextRoadColor` หรือสลับ logic ใน `UpdateHighlight()`

## การหมุนด้วยสองนิ้ว

ถ้าแตะ 2 นิ้วและ `enableTwoFingerRotation` เปิดอยู่

- `HandleTwoFingerRotation()` จะเอาค่า `deltaPosition.x` ของสองนิ้วมาเฉลี่ย
- แล้วนำไปปรับ `yRotationOffset`

ผลคือผู้เล่นสามารถหมุน yaw เพิ่มจากทิศ tangent ของถนนได้

## การกัน UI รบกวน

ถ้าเริ่มแตะบน UI

- `touchStartedOnUI` จะถูกตั้งค่า
- gesture นั้นจะไม่ถูกใช้ควบคุมการเดิน

## สรุปสั้น

`splinemovement` คือระบบเดินบนถนน spline ที่:

- เดินหน้า/ถอยหลังด้วย drag แนวตั้ง คล้าย `dogpaddle`
- detect swipe ซ้าย/ขวาเพื่อเลือกเส้นทางล่วงหน้าตอนใกล้ทางแยก
- เลี้ยวจริงเมื่อวิ่งถึงจุดเปลี่ยนถนน
- วาดเส้น spline ด้วย `LineRenderer`
- ไฮไลต์เส้นทางปัจจุบันและเส้นทางถัดไป

## หมายเหตุเทียบกับคำอธิบายที่ต้องการ

ถ้าอธิบายตามโค้ดจริงตอนนี้ จะตรงประมาณนี้:

- มีการ detect swipe เพื่อเลือกว่าจะไปซ้ายหรือขวา และเป็นการจองทางไว้ก่อน
- การเดินใช้ drag แนวตั้งคล้าย dogpaddle
- ตอนแสดงเส้นทาง ระบบวาด spline ทั้งระบบไว้ และไฮไลต์สี
- แต่สีของเส้นทางที่ "จะไป" ในโค้ดตอนนี้เป็นสีเขียว ไม่ใช่สีเหลือง
